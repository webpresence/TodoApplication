@model ToDoApplication.Models.ToDoListViewModel
@{
    ViewData["Title"] = "Edit ToDo list";
}

@{
    ViewData["Title"] = "Edit";
}

<h1>Edit</h1>

<h4>ToDoItemDto</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Edit">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <input type="hidden" asp-for="ToDoList.Id" />
            <div class="form-group">
                <label asp-for="ToDoList.Name" class="control-label"></label>
                <input asp-for="ToDoList.Name" class="form-control" />
                <span asp-validation-for="ToDoList.Name" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="ToDoList.Description" class="control-label"></label>
                <input asp-for="ToDoList.Description" class="form-control" />
                <span asp-validation-for="ToDoList.Description" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Save" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
